# Repository-wide. Each track keeps its own .gitignore for its own artefacts;
# the dataset track's lives in dataset/.gitignore.
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
kg/.env
kg/out/
kg/mappings/*.ini
kg/.venv/

**/.env
.coverage.*
htmlcov/
**/.venv/
**/.pytest_cache/
**/.coverage
