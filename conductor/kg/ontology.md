# Ontology specification

Normative. `kg/ontology/eat.ttl` implements this file; where they disagree, this file is
the requirement and `eat.ttl` is the bug.

Currently `eat.ttl` is a five-term stub covering only `referral_state`. Completing it is
the first track, and it blocks everything else.

---

## 1. Reused vocabularies

| Vocabulary | Used for |
|---|---|
| **PROV-O** | The whole decision layer. `cites` is `prov:used`; scores, decisions, rule checks and rationales are entities generated by activities associated with versioned agents |
| **SOSA** | Observations. `sosa:Observation`, `observedProperty`, `hasSimpleResult`, `resultTime`, `hasFeatureOfInterest` |
| **OWL-Time** | Suspension intervals — required for `adjustedWaitDays` to be derivable |
| **SKOS** | Every code list in `ref_codes`, plus specialties and CPC |
| **QUDT** | Units, on the observable property |
| **SHACL** | Structural shapes and rule constraints |
| **DCAT / VoID** | Dataset metadata, release tag, seed |

**Deliberately not used.** FHIR RDF — no interop requirement in this challenge and it
would double every class; keep field names MDS-aligned so the mapping stays possible
later. The SNOMED CT ontology — licensed and enormous; reference `sct:` IRIs, import
nothing. FOAF and `org:` — no people or organisations are named in this dataset by design.

## 2. Data-layer classes

| Class | Source table | Notes |
|---|---|---|
| `eat:Person` | `persons` | Only where `ihi_number` is present |
| `eat:Patient` | `patients` | Hospital-scoped; always exists |
| `eat:Referral` | `referral_daily` | The enduring pathway. Never per-day |
| `eat:ReferralState` | `kg.v_referral_state` | One per material change |
| `eat:TriageEvent` | `triage_events` | Once per referral, never per day |
| `eat:CPC` | `ref_codes` | `skos:Concept`, ordered by `eat:severityRank` |
| `eat:TriageOutcome` | `ref_codes` | `skos:Concept` |
| `eat:Condition` | `conditions` | Plus external code IRIs |
| `eat:ObservationEvent` | `observations` | `sosa:Sampling`; one per row |
| `sosa:Observation` | `observations` | One per non-null measured column |
| `sosa:ObservableProperty` | — | Three subclasses, see §5 |
| `eat:SuspensionEvent` | `suspension_events` | First-class, with `time:Interval` |
| `eat:CancellationEvent` | `cancellation_events` | **All** of them, not just the latest |
| `eat:Hospital` | `hospitals` | Six sites |
| `eat:HospitalService` | `hospital_specialty` | n-ary node |
| `eat:Specialty` | `ref_specialty` | `skos:Concept`, HIPE |
| `eat:Ward` | `wards` | |
| `eat:BedAllocation` | `ward_specialty` | n-ary node; gives `nominal_beds` somewhere to live |
| `eat:BedStatus` | `bed_status` | Time-stamped, three a day |
| `eat:ClinicSession` | `clinic_sessions` | Slots, not beds |
| `eat:Rule` | `ref_rules` | Also a `sh:NodeShape` where applicable |

**`eat:HospitalService` is the join the graph must keep.** Cardiology at 9001 is not
cardiology at 9002 — different staff, different waiting list, different beds. Every bed
and slot question hangs off it. Without it, "how full are the cardiology beds for this
patient" silently aggregates across every hospital and returns a plausible, meaningless
number.

## 3. Data-layer properties

| Property | Domain | Range | Card. | Source / why |
|---|---|---|---|---|
| `eat:isRecordOf` | `eat:Patient` | `eat:Person` | 0..1, functional | No IHI → no `Person` node, no placeholder |
| `eat:atHospital` | `eat:Patient`, `eat:Referral` | `eat:Hospital` | 1 | `hospital_hipe` |
| `eat:forPatient` | `eat:Referral` | `eat:Patient` | 1 | `referral_daily.patient_id` |
| `eat:referredToService` | `eat:Referral` | `eat:HospitalService` | 1 | `hospital_hipe` + `specialty_hipe` |
| `eat:stateOf` | `eat:ReferralState` | `eat:Referral` | 1, functional | Type 2 |
| `eat:validFrom` | `eat:ReferralState` | `xsd:date` | 1 | |
| `eat:validTo` | `eat:ReferralState` | `xsd:date` | 0..1 | Absent while current; `removal_date` where removed |
| `eat:hasTriageEvent` | `eat:Referral` | `eat:TriageEvent` | 0..1, functional | Null until returned from triage |
| `eat:assignedCategory` | `eat:TriageEvent` | `eat:CPC` | 0..1 | `triage_category` |
| `eat:outranks` | `eat:CPC` | `eat:CPC` | transitive | `severity_rank`, **never** `code_value` |
| `eat:hasCondition` | `eat:Referral` | `eat:Condition` | 1..n | `is_primary` flags one |
| `eat:icd10amCode` | `eat:Condition` | minted IRI | 1 | ICD-10-AM has no public IRI scheme |
| `eat:snomedCode` | `eat:Condition` | `sct:` IRI | 0..1 | Reference only |
| `eat:hasObservationEvent` | `eat:Referral` | `eat:ObservationEvent` | 0..n | |
| `sosa:hasMember` | `eat:ObservationEvent` | `sosa:Observation` | 1..n | One per non-null column |
| `sosa:observedProperty` | `sosa:Observation` | `sosa:ObservableProperty` | 1 | |
| `sosa:hasFeatureOfInterest` | `sosa:Observation` | `eat:Patient` | 1 | The patient, not the referral |
| `eat:providedBy` | `eat:HospitalService` | `eat:Hospital` | 1 | The unit that owns a waiting list |
| `eat:forSpecialty` | `HospitalService`, `BedAllocation` | `eat:Specialty` | 1 | Both schema-forced joins |
| `eat:inWard` | `eat:BedAllocation` | `eat:Ward` | 1 | `ward_specialty` + `nominal_beds` |
| `eat:statusOf` | `eat:BedStatus` | `eat:Ward` | 1 | |
| `eat:sessionOf` | `eat:ClinicSession` | `eat:HospitalService` | 1 | The real outpatient constraint |
| `eat:hasSuspension` | `eat:Referral` | `eat:SuspensionEvent` | 0..n | First-class, own interval |
| `eat:hasCancellation` | `eat:Referral` | `eat:CancellationEvent` | 0..n | All of them |

## 4. Decision-layer classes and properties

| Property | Domain | Range | Card. | Source / why |
|---|---|---|---|---|
| `eat:scored` | `eat:Score` | `eat:Referral` | 1 | `agent_scores` |
| `eat:scoreValue` | `eat:Score` | `xsd:decimal` 0–1 | 1 | Never stored without method + version |
| `eat:method` / `eat:agentVersion` | `eat:Score` | `xsd:string` | 1 / 1 | Reproducibility, not decoration |
| `eat:cites` | `Score`, `RankedPlacement` | evidence node | 1..n | `rdfs:subPropertyOf prov:used` |
| `eat:citesUrgencyEvidence` | ⊑ `eat:cites` | evidence node | 0..n | `role = urgency` |
| `eat:citesCapacityEvidence` | ⊑ `eat:cites` | evidence node | 0..n | `role = capacity` |
| `eat:citesTimeframeEvidence` | ⊑ `eat:cites` | `ReferralState`, `Rule` | 0..n | `role = timeframe` |
| `eat:citesMultiListEvidence` | ⊑ `eat:cites` | `eat:Referral` | 0..n | `role = multi_list`; needs an IHI |
| `eat:hasPlacement` | `eat:Decision` | `eat:RankedPlacement` | 1..n | `decision_rankings` |
| `eat:position` | `eat:RankedPlacement` | `xsd:positiveInteger` | 1 | Unique per decision + category |
| `eat:ranks` | `eat:RankedPlacement` | `eat:Referral` | 1 | |
| `eat:withinCategory` | `eat:RankedPlacement` | `eat:CPC` | 0..1 | Stored so `RULE-ORDER` is checkable directly |
| `eat:testedRule` | `eat:RuleCheck` | `eat:Rule` | 1 | Pass and fail both stored |
| `eat:passed` | `eat:RuleCheck` | `xsd:boolean` | 1 | A violation becomes a fact, not a log line |
| `eat:revises` | `eat:Override` | `eat:RankedPlacement` | 1 | `from_position` → `to_position` |
| `eat:reason` | `eat:Override` | `xsd:string` | 1 | **No override without one** |
| `eat:acceptedWarning` | `eat:Override` | `eat:RuleCheck` | 0..n | Warning and the decision to proceed, both kept |
| `prov:wasGeneratedBy` | `Score`, `Decision`, `RuleCheck` | `prov:Activity` | 1 | Every written node has one |
| `prov:wasAssociatedWith` | `prov:Activity` | `prov:Agent` | 1 | Agent or clinician, never anonymous |
| `prov:wasDerivedFrom` | `eat:Rationale` | `eat:RankedPlacement` | 1 | Text may cite nothing the placement did not |
| `eat:rationaleText` | `eat:Rationale` | `xsd:string` | 1 | Chosen over overloading `prov:value` |
| `prov:used` | `eat:Rationale` | citation set | 1..n | Exactly what was in the prompt |
| `prov:wasAttributedTo` | `eat:Rationale` | `prov:SoftwareAgent` | 1 | The model, by version string |
| `eat:promptHash` | `eat:Rationale` | `xsd:string` | 1 | sha256 |

**A score is never a bare number.** It is an entity with `prov:wasGeneratedBy` an
activity, a versioned agent, and one `eat:cites` edge per evidence row.

**The `role` column of `decision_citations` becomes four sub-properties, not a reified
statement.** One query gets the whole explanation; the same query with a sub-property gets
one section of the clinician's expander.

## 5. Observation subclasses

The three kinds the dataset keeps apart become subclasses of `sosa:ObservableProperty`:

| Subclass | Columns | Produced by |
|---|---|---|
| `eat:MeasuredVital` | `hr`, `sbp`, `dbp`, `rr`, `temp`, `spo2` | A machine or a nurse |
| `eat:ReportedSymptom` | `pain`, `chiefcomplaint` | The patient |
| `eat:ClinicalAssessment` | `avpu`, `news2`, `mts_category`, `icts_category` | Clinician, or arithmetic |

Clinicians keep measured and reported information strictly apart because one is evidence
and the other is testimony. The distinction must survive into the graph.

## 6. CPC ordering

Severity order lives in the graph, not in code:

```turtle
eat:CPCScheme a skos:ConceptScheme ;
    skos:prefLabel "Clinical Prioritisation Category" .

<…/kg/id/cpc/1> a skos:Concept ;      # Urgent
    skos:inScheme eat:CPCScheme ; skos:notation "1" ;
    eat:severityRank 1 ; eat:crtDays 28 .
<…/kg/id/cpc/3> a skos:Concept ;      # Semi-Urgent
    eat:severityRank 2 ; eat:crtDays 91 .
<…/kg/id/cpc/2> a skos:Concept ;      # Routine
    eat:severityRank 3 .              # no crtDays

eat:outranks a owl:ObjectProperty, owl:TransitiveProperty ;
    rdfs:domain eat:CPC ; rdfs:range eat:CPC .
<…/kg/id/cpc/1> eat:outranks <…/kg/id/cpc/3> , <…/kg/id/cpc/2> .
<…/kg/id/cpc/3> eat:outranks <…/kg/id/cpc/2> .
```

Note the code values: Urgent 1, Routine 2, Semi-Urgent **3**. Anything that sorts by
`code_value` puts Routine ahead of Semi-Urgent, silently. Use `eat:severityRank`.

## 7. Not in the graph

- **`eval.ground_truth`** — no triples, in any graph, under any circumstances. Enforced by
  the `kg_loader` role having no grant on schema `eval`.
- Names, addresses, phone numbers, emails — not in the dataset by design.
- GP and consultant identities — not stored. A ranking should not know whose patient it is.
