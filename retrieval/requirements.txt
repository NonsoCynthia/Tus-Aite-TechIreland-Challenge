# Bounds on first install; pin exact versions once the install is known good,
# matching dataset/requirements.txt's convention.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5
